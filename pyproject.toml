[project]
name = "kiku-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [ "kiku-ai" ]


[tool.uv.sources]
kiku-ai = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

 [tool.uv]
 package = false
